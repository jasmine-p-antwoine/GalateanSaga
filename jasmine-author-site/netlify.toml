[build]
  base = "jasmine-author-site"
  publish = "jasmine-author-site/public"
  command = "cd jasmine-author-site && hugo --gc --minify"

[build.environment]
  HUGO_VERSION = "0.153.3"

[context.production.environment]
  HUGO_ENV = "production"